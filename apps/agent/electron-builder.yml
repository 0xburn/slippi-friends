appId: com.friendlies.agent
productName: friendlies
copyright: Copyright © 2026

files:
  - "dist/**/*"
  - "assets/**/*"
  - "!dist/renderer/src"

extraMetadata:
  main: dist/main.js

mac:
  category: public.app-category.social-networking
  target:
    - dmg
    - zip
  hardenedRuntime: true
  icon: assets/icon.png

win:
  target:
    - nsis
    - portable
  icon: assets/icon.png

nsis:
  oneClick: true
  perMachine: false
  allowToChangeInstallationDirectory: false
  artifactName: "friendlies-Setup-${version}.${ext}"

portable:
  artifactName: "friendlies-${version}-portable.exe"

linux:
  target:
    - AppImage
  category: Network
  icon: assets/icon.png

protocols:
  - name: slippi-friends
    role: Viewer
    schemes:
      - slippi-friends

publish:
  provider: github
  owner: 0xburn
  repo: slippi-friends

directories:
  output: release
